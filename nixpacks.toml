[phases.setup]
nixPkgs = ["python312"]

[phases.install]
cmds = ["curl -LsSf https://astral.sh/uv/install.sh | sh", "cd projects/backend && uv sync --frozen"]

[start]
cmd = "cd projects/backend && uv run uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"
